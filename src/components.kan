import "str";
import "text";
import "ecs" as e;
import "physics";

type ComponentKind enum {
    Transform,
    Hitbox,
    AnimatedGraphics,
    Sprite,
    Input,
    Movement,
    Interaction,
    TextBox
}

type Input struct {
    last_interact: i64,
    interacting: bool
}

type TextBox struct {
    dialog_text: str.View,
    chars_shown: usize,
    textbox: *text.TextBox
}

type Interaction struct {
    hitbox: physics.Hitbox,
    last_state: usize,
    current_state: usize
}
